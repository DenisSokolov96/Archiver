# Huffman Archiver 📦

Простая реализация архиватора на языке Python, использующая алгоритм **кодирования Хаффмана**. Поддерживается потоковая обработка данных, кастомная 
сериализация деревьев и проверка целостности файлов.

## ✨ Особенности
*   **Сжатие без потерь (Lossless):** Алгоритм Хаффмана гарантирует 100% восстановление данных.
*   **Экономия памяти:** Чтение и запись файлов производятся фрагментами (chunks), что позволяет обрабатывать файлы любого размера.
*   **Целостность данных:** Встроенная поддержка MD5-хеширования для сравнения оригинала и распакованного файла.
*   **Двухочередное построение дерева:** Оптимизированный алгоритм создания дерева Хаффмана с использованием `collections.deque`.
*   **Инкапсуляция:** Строгая структура узлов (`Node`).

## 🛠 Технический стек
*   **Язык:** Python 3.10+
*   **Алгоритмы:** Huffman Coding, Quick Sort, MD5 Hashing.
*   **Формат данных:** Бинарный с кастомным заголовком.

## 📖 Структура архива (.sar)

При сжатии создается бинарный файл со следующей структурой:
1.  **Header (4 байта):** Длина (Big-endian) сериализованной таблицы кодов.
2.  **Metadata:** Сериализованная таблица (байт -> префиксный код).
3.  **Padding (1 байт):** Количество неиспользуемых бит в последнем байте данных.
4.  **Payload:** Сжатый битовый поток.
